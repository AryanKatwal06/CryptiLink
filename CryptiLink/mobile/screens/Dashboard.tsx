import React from 'react';
import { View, Text } from 'react-native';

const Dashboard: React.FC = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>CryptiLink Dashboard</Text>
    </View>
  );
};

export default Dashboard;
