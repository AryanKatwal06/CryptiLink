import React from 'react';
import { View, Text } from 'react-native';

const SplashScreen: React.FC = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>CryptiLink — Splash</Text>
    </View>
  );
};

export default SplashScreen;
