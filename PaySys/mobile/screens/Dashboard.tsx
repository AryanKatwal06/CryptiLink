import React from 'react';
import { View, Text } from 'react-native';

const Dashboard: React.FC = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>PaySys Dashboard</Text>
    </View>
  );
};

export default Dashboard;
