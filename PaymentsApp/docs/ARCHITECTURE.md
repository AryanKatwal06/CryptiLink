# Architecture Overview

This repository follows a Modular Monolith architecture. Mobile is React Native CLI; backend is Java 21 + Spring Boot. Shared contracts live under `shared/` and are the single source of truth for DTOs and enums.
