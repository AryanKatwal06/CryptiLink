{
  "name": "secure-wallet-application",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "CryptiLink"
  ],
  "scripts": {
    "prepare": "npm --prefix CryptiLink run prepare",
    "dev": "npm --prefix CryptiLink run dev",
    "build": "npm --prefix CryptiLink run build",
    "test": "npm --prefix CryptiLink run test",
    "clean": "npm --prefix CryptiLink run clean",
    "lint": "npm --prefix CryptiLink run lint",
    "format": "npm --prefix CryptiLink run format",
    "verify": "npm --prefix CryptiLink run verify",
    "setup": "npm --prefix CryptiLink run setup"
  },
  "devDependencies": {
    "typescript": "5.1.6"
  }
}
