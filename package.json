{
  "name": "secure-wallet-application",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "PaySys"
  ],
  "scripts": {
    "prepare": "npm --prefix PaySys run prepare",
    "dev": "npm --prefix PaySys run dev",
    "build": "npm --prefix PaySys run build",
    "test": "npm --prefix PaySys run test",
    "clean": "npm --prefix PaySys run clean",
    "lint": "npm --prefix PaySys run lint",
    "format": "npm --prefix PaySys run format",
    "verify": "npm --prefix PaySys run verify",
    "setup": "npm --prefix PaySys run setup"
  },
  "devDependencies": {
    "typescript": "5.1.6"
  }
}
