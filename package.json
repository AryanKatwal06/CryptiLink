{
  "name": "secure-wallet-application",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "PaymentsApp"
  ],
  "scripts": {
    "prepare": "npm --prefix PaymentsApp run prepare",
    "dev": "npm --prefix PaymentsApp run dev",
    "build": "npm --prefix PaymentsApp run build",
    "test": "npm --prefix PaymentsApp run test",
    "clean": "npm --prefix PaymentsApp run clean",
    "lint": "npm --prefix PaymentsApp run lint",
    "format": "npm --prefix PaymentsApp run format",
    "verify": "npm --prefix PaymentsApp run verify",
    "setup": "npm --prefix PaymentsApp run setup"
  }
  ,"devDependencies": {
    "typescript": "5.1.6"
  }
}
